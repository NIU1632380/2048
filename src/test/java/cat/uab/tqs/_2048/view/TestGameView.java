/* package cat.uab.tqs._2048.view;

import org.junit.jupiter.api.Test;
import static org.mockito.Mockito.*;

public class MockGameView {

    @Test
    void testMockGameView() {
        // Crear el mock de MockGameView
        MockGameView mockView = mock(MockGameView.class);
        // Simular métodos
        doNothing().when(mockView).updateBoard();
        doNothing().when(mockView).showMessage("Game Over!");

        // Interactuar con el mock
        mockView.updateBoard();
        mockView.showMessage("Game Over!");

        // Verificar que los métodos se han llamado
        verify(mockView).updateBoard();
        verify(mockView).showMessage("Game Over!");
    }
}
 */

